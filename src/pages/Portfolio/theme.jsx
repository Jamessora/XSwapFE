// theme.jsx
import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    success: {
      main: '#4caf50',
      light: '#81c784',
      dark: '#388e3c'
    }
  },
  
});

export default theme;
